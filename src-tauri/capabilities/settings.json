{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "settings pop-out window — local read-only config; no cross-origin fetch, no window creation",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "log:default"
  ]
}
